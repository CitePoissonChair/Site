# Netlify configuration for CPC React site

[build]
  command = "npm run build"
  publish = "dist"

[env]
  NODE_VERSION = "20"

# Redirect all routes to index.html for React Router (client-side routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
